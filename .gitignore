# dependencies
# This folder contains all the third-party libraries. It can be huge
# and should always be ignored. Anyone can reinstall them using `npm install`.
/node_modules

# build output
# This folder contains the final, optimized code for production. It's generated
# every time you run `npm run build`, so there's no need to track it.
/dist
/dist-ssr

# log files
# These are auto-generated log files for debugging.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
# This is CRITICAL for security. These files store sensitive information like
# API keys and passwords. They should NEVER be committed to a public repository.
.env
.env.local
.env.*.local

# Vite-specific cache and build directories
.vite/
.output

# editor and OS files
# These are configuration and metadata files generated by your code editor or OS.
# They are specific to your local machine and shouldn't be shared.
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

README.md